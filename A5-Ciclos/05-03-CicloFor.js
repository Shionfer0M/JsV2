//TODO: Ciclo For
let contador = 0;
for (contador; contador < 3; contador++) {
  console.log(contador);
}
console.log("Fin del ciclo for");
//! Tambiíen se puede declarar así:
//! for(let contador=0; contador<3; contador++)
//* Declaramor las variables let a dentro del for
//! La diferencia con el while esque la variable de contador esta a dentro en el for contador++
